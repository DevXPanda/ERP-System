# TypeScript Rules

- Never use `any` or `as any`.
- Always use strict TypeScript.
- Fix the root cause instead of suppressing type errors.
- Use Convex generated types (Id<"table">, Doc<"table">) wherever applicable.
- If a type cannot be inferred, create a proper interface or type instead of using `any`.
